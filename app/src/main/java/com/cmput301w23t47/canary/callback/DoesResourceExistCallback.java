package com.cmput301w23t47.canary.callback;

/**
 * The interface Does resource exist callback.
 */
public interface DoesResourceExistCallback {
    /**
     * Does resource exists.
     *
     * @param exists the exists
     */
    void doesResourceExists(boolean exists);
}
